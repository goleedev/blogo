---
title: '🖌️ 리액트란'
thumbSrc: '/static/images/react1.png'
date: '2021-08-11'
tags: ['FE', 'React']
draft: false
summary: '리액트를 학습하며 정리한 자료로 리액트란 무엇인지 설명합니다.'
images: ['/static/images/react1.png']
---

# 📌 목차

![React](/static/images/react1.png)

- **💁🏻 리액트란**

- **🤔 페이스북이 리액트를 만든 이유**

- **🔮 가상 돔이란**

---

# 💁🏻 리액트란

페이스북에서 개발한 `컴포넌트`라는 개념에 집중이 되어있는 라이브러리입니다. 간단히 말해, 컴포넌트는 **데이터를 넣으면 지정한 유저 인터페이스를 조립**해서 보여주는 것을 말하는데요. 리액트는 여러가지 장점과 특징을 가지는데, 특히 **생태계가 굉장히 넓고, 사용하는 곳이 많습니다**. 또한, `HTTP 클라이언트`, `라우터`, `심화적 상태 관리` 등의 **기능들은 내장되어있지 않습니다**. 그리고, 이처럼 내장되지 않은 기능은 공식 라이브러리가 있지 않아, 개발자가 원하는 스택을 맘대로 골라서 사용하거나 직접 라이브러리를 제작하여 사용할 수 있습니다. 즉, **자유도가 높다**는 것입니다

---

# 🤔 페이스북이 리액트를 만든 이유

페이스북이 리액트를 만들기 전에도 다양한 프레임워크가 존재헀는데, 데이터단을 담당하는 `모델 (Model)`, 사용자의 화면에서 보여지게 되는 `뷰 (View)`, 그리고 사용자가 발생시키는 이벤트를 처리해주는 `컨트롤러 (Controller)`로 이루어진 `MVC 패턴`, 그리고 MVC에서부터 파생된 `MVVM (View Model)`, `MVW (Whatever)` 등의 패턴들로 이루어져 있었습니다. 여기서 해당 프레임워크들의 공통점은 **모델**으로, **양방향 바인딩을 통하여 모델에 있는 값이 변하면 뷰에서도 이를 변화**시켜줍니다. 여기서 핵심적인 부분은 **변화시켜준다는 부분**인데요. 즉, 일단 첫 화면을 띄워주고 **변화에 따라 필요한곳을 바꿔주는 것**입니다.

여기서 `변화 (Mutation)`라는것은 상당히 복잡한 작업인데, 특정 이벤트가 발생했을때 **모델에 변화**를 일으키고, 변화를 일으킴에 따라 **어떤 DOM을 가져와서 어떠한 방식으로 뷰를 업데이트 해줄 지 로직**을 정해줘야 하는데요.

![React View](/static/images/react2.gif)

이때, 페이스북은 리액트를 만들면서 **데이터가 바뀌면 그냥 뷰를 날려버리고 새로 만들어버리자**는 생각을 하게 됩니다. 하지만, 그때 그때 새로 뷰를 만들어버리려고 한다면 성능적으로 엄청난 문제가 생길 수 있기 때문에, 이떄 **`가상 돔 (Virtual DOM)`을 사용**하게 됩니다.

---

# 🔮 가상 돔이란

![Virtual Dom](/static/images/react-virtual-dom.png)

`Virtual DOM`은 가상의 DOM을 의미하는데요. 변화가 일어나면 실제로 브라우저의 DOM에 새로운 것을 넣는 것이 아니라, 자바스크립트로 이뤄진 **가상 DOM에 한번 렌더링을 하고 기존의 DOM과 비교를 한 다음에 변화가 필요한 곳에만 업데이트**를 해주는 것입니다.

이러한 `Virtual DOM`을 사용함으로서, 데이터가 바뀌었을 때 어떻게 업데이트 할 지를 고려하는 것이 아닌, **바뀐 데이터로 그려놓고 비교를 한 다음에 바뀐 부분만 찾아서 업데이트** 하게 됩니다. 즉, `Virtual DOM`은 **DOM 변화를 최소화 시켜주는 역할**을 하게 됩니다.

---

### 🔗 References

[React 문서](https://ko.reactjs.org/docs/getting-started.html)

[Introduction to React.js](https://www.youtube.com/watch?v=XxVg_s8xAms)

[누구든지 하는 리액트 1편: 리액트는 무엇인가](https://velopert.com/3612)
