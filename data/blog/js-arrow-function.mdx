---
title: '😎 자바스크립트 화살표 함수란'
thumbSrc: '/static/images/js34.png'
date: '2021-08-01'
tags: ['FE', 'JavaScript']
draft: false
summary: '모던 자바스크립트를 학습하며 정리한 자료로 화살표 함수에 대해 설명합니다.'
images: ['/static/js33/js34.png']
authors: ['default']
---

# 📌 목차

![JS Arrow Function](/static/images/js34.png)

- **💁🏻 화살표 함수란**

- **✨ 화살표함수의 특징**

- **❌ 화살표 함수를 사용할 수 없는 경우**

- **🏹 화살표 함수와 this**

---

# 💁🏻 화살표 함수란

화살표 함수란 `function` 키워드 없이 `=>` 키워드를 이용해 함수를 생성하는 방법입니다. 화살표함수는 `return`이 없어도 함수 실행을 종료시키고 값을 반환하며, 화살표함수를 이용해 일반 함수를 아래와 같이 표현할 수 있다.

```javascript
// 일반 함수
const sum = function (x, y) {
  return x + y
}

sum(3, 4) //7

// 화살표 함수
const sum = (x, y) => x + y

sum(3, 4) //7
```

---

# ✨ 화살표함수의 특징

화살표 함수 표현 (arrow function expression)은 function 표현에 비해 구문이 짧고 **자신의 `this`, `arguments`, `super` 또는 `new.target`을 바인딩 하지 않습니다**. 또한 화살표 함수는 항상 익명이고, 이 함수 표현은 **메소드 함수가 아닌 곳에 가장 적합**합니다. 즉, **생성자로서 사용할 수 없습니다**.

---

# ❌ 화살표 함수를 사용할 수 없는 경우

화살표 함수를 사용할 수 없는 경우는 아래와 같이 세가지가 있습니다.

- **객체의 메소드를 정의**하는 경우

- **`prototype`에 메소드를 할당**하는 경우

- **생성자 함수로 사용**하는 경우

---

# 🏹 화살표 함수와 this

함수가 어떻게 호출되었는지에 따라 바인딩할 객체가 결정되는 일반함수와는 달리, 화살표 함수의 this는 **화살표 함수가 호출되는 시점과는 무관하게 선언되는 시점에 결정되며 언제나 `상위 스코프의 this`** 를 가리킵니다. 이는 화살표 함수에는 `this`와 `argument`가 없기 때문인데요. 따라서 일반적으로 `this`가 개입되는 경우라면 일반 함수를 사용한다.

---

### 🔗 Reference

[MDN 화살표 함수](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Functions/Arrow_functions)
