---
title: '😎 자바스크립트 함수 표현식과 함수 선언식란'
thumbSrc: '/static/images/js36.png'
date: '2021-08-06'
tags: ['FE', 'JavaScript']
draft: false
summary: '모던 자바스크립트를 학습하며 정리한 자료로 함수 표현식과 함수 선언식에 대해 설명합니다.'
images: ['/static/images/js36.png']
authors: ['default']
---

# 📌 목차

![JS Function Expression](/static/images/js36.png)

- **💁🏻 함수 선언식이란**

- **📢 함수 표현식이란**

- **⚖️ 함수 선언식과 표현식의 차이점**

- **👍 함수 표현식의 장점**

---

# 💁🏻 함수 선언식이란

`함수 선언식 (Function Declarations)`은 일반적인 프로그래밍 언어에서의 **함수 선언과 비슷한 형식**을 말하며, 아래와 같은 형식을 가지고 있습니다.

```javascript
function funcDeclarations() {
  return 'A function declaration'
}

funcDeclarations()
```

---

# 📢 함수 표현식이란

`함수 표현식 (Function Expressions)`은 유연한 **자바스크립트 언어의 특징을 활용한 선언 방식**으로, 아래와 같은 형식을 가지고 있습니다.

```javascript
const funcExpression = function () {
  return 'A function expression'
}

funcExpression()
```

---

# ⚖️ 함수 선언식과 표현식의 차이점

함수 선언식은 **호이스팅에 영향**을 받지만, 함수 표현식은 호이스팅에 영향을 받지 않습니다. 자세히 말하자면, 함수 선언식은 **코드를 구현한 위치와 관계없이 자바스크립트의 특징인 호이스팅에 따라 브라우저가 자바스크립트를 해석할 때 맨 위로 끌어 올려집니다**.

---

# 👍 함수 표현식의 장점

**함수 표현식이 호이스팅에 영향을 받지 않는다**는 특징 이외에도 함수 선언식보다 유용하게 쓰이는 경우는 다음과 같은데요.

- **클로져로 사용이 가능**합니다.

- **콜백으로 사용** (다른 함수의 인자로 넘길 수 있음)이 가능합니다.

## 함수 표현식으로 클로져 생성하기

클로져는 **함수를 실행하기 전에 해당 함수에 변수를 넘기고 싶을 때** 사용됩니다.

```javascript
function tabsHandler(index) {
  return function tabClickEvent(event) {
    console.log(index)
  }
}
```

## 함수 표현식을 다른 함수의 인자 값으로 넘기기

```javascript
const arr = ['a', 'b', 'c']

arr.forEach(function () {
  // ...
})
```

---

### 🔗 References

[MDN 함수 표현식](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Operators/function)

[MDN 함수 선언](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Statements/function)
